name: Build

on:
  workflow_dispatch:
    inputs:
      project:
        description: "Name of the project to build"
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ inputs.project }}
